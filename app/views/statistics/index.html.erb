<h1>全体情報</h1>
<div class="table-responsive">
  <table class="table table-striped table-bordered">
    <tr>
      <th>全写真枚数</th>
      <th>タグ付け済み枚数</th>
      <th>タグ付け未済み枚数</th>
    </tr>
    <tr>
      <td><%= @allCount %></td>
      <td><%= @tagedCount %></td>
      <td><%= @notTagedCount %></td>
    </tr>
  </table>
</div>
<br>
<h1>登場回数ランキング</h1>
(↑↓の付いているヘッダタップでソートできます。)
<div class="table-responsive">
  <table id="statistics" class="table table-striped table-bordered">
  	<thead>
  	  <tr>
  	  	<th rowspan="2">No.↑↓</th>
  	  	<th rowspan="2">名前↑↓</th>
  	  	<th colspan="3">全体統計</th>
  	  	<th colspan="3">自身アップロード分統計</th>
  	  </tr>
      <tr>


        <th>登場枚数↑↓</th>
        <th>自撮り枚数↑↓</th>
        <th>自撮り枚数/登場枚数↑↓</th>
        <th>アップロード枚数↑↓</th>
        <th>自撮り枚数↑↓</th>
        <th>自撮り枚数/登場枚数↑↓</th>
      </tr>
    </thead>
    <tbody>
      <% @members.each do |m| %>
        <tr>
          <td><%= m.id %></td>
          <td><%= m.name %></td>
          <td><%= @ranks[m.id][:count] %></td>
          <td><%= @ranks[m.id][:soloCount] %></td>
          <td><%= @ranks[m.id][:ratio] %>%</td>
          <td><%= @ranks[m.id][:countByOwner] %></td>
          <td><%= @ranks[m.id][:soloCountByOwner] %></td>
          <td><%= @ranks[m.id][:ratioByOwner] %>%</td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div><script type="text/javascript">
$('table#statistics').dataTable({
  
    "bPaginate": false,
    "bFilter": false,
    "bSort": true,
    "bInfo": false,
    "bAutoWidth": false	
});
</script>

<br>
<h1>2ショット枚数</h1>
<div class="table-responsive">
  <table class="table table-striped table-bordered">
  	<thead>
  	  <tr>
  	  	<th></th>
  	  	<% @members.each do |m| %>
  	  	  <th class="tategaki"><%=m.name %></th>
  	  	<% end %>
  	  </tr>
    </thead>
    <tbody>
      <% @members.each do |m| %>
        <tr>
          <td><%= m.name %></td>
          <% @members.each do |m2| %>
            <% if m.id == m2.id %>
              <td></td>
            <% else %>
              <% if m.id < m2.id
                   count = @counts[m.id][m2.id]
                 else
                   count = @counts[m2.id][m.id]
                 end	
              %>
              <% if count == 0  %>
                <td class="zero">0</td>
              <% else %>
                <td>
                  <%= count %>
                </td>
              <% end %>
            <% end %>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>